require 'hand'

describe Hand do
  subject(:hand) {Hand.new}
  context "#initialize"
    it "check hand is empty when initialized" do
      expect(hand)
    end
    it "hand contains 5 cards"
    it "calculates hand"
    it "check if hand is part of list of plays"

end
